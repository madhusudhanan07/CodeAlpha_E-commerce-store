/**
 * services/index.js — Service Layer Registry (Placeholder)
 *
 * Services encapsulate business logic and sit between controllers and models.
 * Controllers call services; services call models/DB.
 * This keeps controllers thin and logic testable in isolation.
 *
 * Convention:
 *   - One file per domain (e.g., userService.js, productService.js)
 *   - Each service exports pure functions or a class instance
 *
 * Example (to be added in Phase 2):
 *   export { default as userService }    from './userService.js';
 *   export { default as productService } from './productService.js';
 */

// No services yet — feature implementation pending.
